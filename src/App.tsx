export default function App() {
  return (
    <main className="app">
      <h1>renderCV Web</h1>
      <p>Select a RenderCV YAML file to generate your PDF entirely in the browser.</p>
    </main>
  )
}